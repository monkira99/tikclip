/** Shared layout for `FlowCanvas` SVG edges and `FlowCanvasNode` boxes — must stay in sync. */
export const FLOW_CANVAS_NODE_W = 220;
export const FLOW_CANVAS_NODE_H = 216;
/** Matches `p-6` on the canvas shell (SVG origin vs. node `left` / `top`). */
export const FLOW_CANVAS_PAD = 24;
